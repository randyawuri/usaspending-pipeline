name: usaspending_pipeline
version: "1.0.0"
config-version: 2

profile: usaspending_pipeline

model-paths: ["models"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  usaspending_pipeline:
    staging:
      +materialized: view
    marts:
      +materialized: table